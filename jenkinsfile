pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                sh 'python3  /tmp/print_info.py'
                sh 'echo good'
            }
        }
    }
}
